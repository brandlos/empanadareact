import React, { Component } from "react";

class Menus extends Component {
  render() {
    return (
      <p className="Content">
        This component will hold image-links to the currently available menus.
      </p>
    );
  }
}

export default Menus;
